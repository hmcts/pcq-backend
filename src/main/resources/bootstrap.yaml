spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/pcq
      aliases:
        pcq-backend-POSTGRES-PASS: PCQ_DB_PASSWORD
        flyway-password: FLYWAY_PASSWORD
