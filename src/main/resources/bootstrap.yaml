spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/pcq
      aliases:
        #backend-POSTGRES-PASS: PCQ_DB_PASSWORD
        backend-POSTGRES-PASS: spring.datasource.password
        flyway-password: flyway.password
